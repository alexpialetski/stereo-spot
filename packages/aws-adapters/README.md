# stereo-spot-aws-adapters

AWS implementations of JobStore, SegmentCompletionStore, QueueSender/Receiver, ObjectStorage (DynamoDB, SQS, S3).

**Main targets:** `test`, `lint`, `smoke-test`.

**Full documentation:** [Packages → aws-adapters](https://alexpialetski.github.io/stereo-spot/docs/packages/overview#aws-adapters). For local docs preview: `nx start stereo-spot-docs`.
