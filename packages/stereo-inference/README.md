# stereo-inference

Inference container (iw3/nunif) for 2D→stereo conversion; used by SageMaker or as HTTP server.

**Main targets:** `test`, `lint`, `inference-build`, `inference-redeploy`.

**Full documentation:** [Packages → stereo-inference](https://alexpialetski.github.io/stereo-spot/docs/packages/overview#stereo-inference). For local docs preview: `nx start stereo-spot-docs`.
