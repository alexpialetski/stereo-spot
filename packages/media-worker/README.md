# Media worker

Chunking and reassembly (ffmpeg); consumes chunking and reassembly queues.

**Main targets:** `test`, `lint`, `build`, `deploy`.

**Full documentation:** [Packages → media-worker](https://alexpialetski.github.io/stereo-spot/docs/packages/overview#media-worker). For local docs preview: `nx start stereo-spot-docs`.
