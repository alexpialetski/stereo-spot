{
  "name": "web-ui",
  "$schema": "../../node_modules/nx/schemas/project-schema.json",
  "projectType": "application",
  "sourceRoot": "packages/web-ui",
  "implicitDependencies": ["shared-types", "aws-adapters"],
  "targets": {
    "build": {
      "executor": "nx:run-commands",
      "options": {
        "command": "docker build -f packages/web-ui/Dockerfile -t stereo-spot-web-ui:latest .",
        "cwd": "{workspaceRoot}"
      },
      "configurations": {},
      "outputs": []
    },
    "deploy": {
      "executor": "nx:run-commands",
      "options": {
        "command": ". packages/aws-infra/.env && docker tag stereo-spot-web-ui:latest \"${ECR_WEB_UI_URL}:latest\" && docker push \"${ECR_WEB_UI_URL}:latest\" && aws ecs update-service --cluster \"$ECS_CLUSTER_NAME\" --service web-ui --force-new-deployment --region us-east-1",
        "cwd": "{workspaceRoot}"
      },
      "configurations": {},
      "dependsOn": ["aws-infra:terraform-output", "web-ui:build"],
      "inputs": ["default", "{workspaceRoot}/packages/aws-infra/.env"]
    },
    "test": {
      "executor": "nx:run-commands",
      "options": {
        "command": "pip install -e ../shared-types -q && pip install -e ../aws-adapters -q && pip install -e . -q && pytest -v",
        "cwd": "{projectRoot}"
      },
      "dependsOn": ["^build"],
      "configurations": {}
    },
    "lint": {
      "executor": "nx:run-commands",
      "options": {
        "command": "ruff check src tests",
        "cwd": "{projectRoot}"
      },
      "configurations": {}
    },
    "serve": {
      "executor": "nx:run-commands",
      "options": {
        "command": "export STEREOSPOT_ENV_FILE=\"$PWD/packages/aws-infra/.env\" && cd packages/web-ui && pip install -e ../shared-types -q && pip install -e ../aws-adapters -q && pip install -e . -q && uvicorn stereo_spot_web_ui.main:app --reload",
        "cwd": "{workspaceRoot}"
      },
      "configurations": {}
    }
  }
}
