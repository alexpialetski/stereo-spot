# StereoSpot Web UI

FastAPI server-rendered UI: dashboard, job creation, jobs list, job detail with upload or video player, and live progress via SSE.

**Main targets:** `serve`, `test`, `lint`, `build`, `deploy`.

**Full documentation:** [Packages → web-ui](https://alexpialetski.github.io/stereo-spot/docs/packages/overview#web-ui). For local docs preview: `nx start stereo-spot-docs`.
