# shared-types

Python library: Pydantic models and cloud-agnostic interfaces (JobStore, SegmentCompletionStore, queues, ObjectStorage). Single source of truth for job and segment shapes.

**Main targets:** `test`, `lint`.

**Full documentation:** [Packages → shared-types](https://alexpialetski.github.io/stereo-spot/docs/packages/overview#shared-types) and [Shared types and interfaces](https://alexpialetski.github.io/stereo-spot/docs/architecture/shared-types). For local docs preview: `nx start stereo-spot-docs`.
