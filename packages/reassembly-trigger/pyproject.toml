[build-system]
requires = ["setuptools>=61", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "stereo-spot-reassembly-trigger"
version = "0.1.0"
description = "Lambda: DynamoDB Stream (SegmentCompletions) → conditional create ReassemblyTriggered → send job_id to reassembly queue"
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
    "stereo-spot-shared",
]

[project.optional-dependencies]
dev = [
    "pytest>=7.0",
    "ruff>=0.1.0",
]

[tool.setuptools.packages.find]
where = ["src"]

[tool.pytest.ini_options]
testpaths = ["tests"]
pythonpath = ["src"]

[tool.ruff]
line-length = 100
target-version = "py310"

[tool.ruff.lint]
select = ["E", "F", "I", "UP"]
