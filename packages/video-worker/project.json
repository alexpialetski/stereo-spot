{
  "name": "video-worker",
  "$schema": "../../node_modules/nx/schemas/project-schema.json",
  "projectType": "application",
  "sourceRoot": "packages/video-worker",
  "implicitDependencies": ["shared-types", "aws-adapters", "adapters"],
  "targets": {
    "build": {
      "executor": "nx:run-commands",
      "options": {
        "command": "docker build -f packages/video-worker/Dockerfile -t stereo-spot-video-worker:latest .",
        "cwd": "{workspaceRoot}"
      },
      "configurations": {},
      "outputs": []
    },
    "deploy": {
      "executor": "nx:run-commands",
      "options": {
        "command": ". packages/aws-infra/.env && docker tag stereo-spot-video-worker:latest \"${ECR_VIDEO_WORKER_URL}:latest\" && docker push \"${ECR_VIDEO_WORKER_URL}:latest\" && aws ecs update-service --cluster \"$ECS_CLUSTER_NAME\" --service video-worker --force-new-deployment --region us-east-1",
        "cwd": "{workspaceRoot}"
      },
      "configurations": {},
      "dependsOn": ["aws-infra:terraform-output", "video-worker:build"],
      "inputs": ["default", "{workspaceRoot}/packages/aws-infra/.env"]
    },
    "test": {
      "executor": "nx:run-commands",
      "options": {
        "command": "pytest -v",
        "cwd": "{projectRoot}"
      },
      "configurations": {},
      "dependsOn": ["stereo-spot:install-deps"]
    },
    "lint": {
      "executor": "nx:run-commands",
      "options": {
        "command": "ruff check src tests",
        "cwd": "{projectRoot}"
      },
      "configurations": {},
      "dependsOn": ["stereo-spot:install-deps"]
    }
  }
}
